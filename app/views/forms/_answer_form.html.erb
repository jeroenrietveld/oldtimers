<%= form_for @question do |f| %>
	<%= render 'shared/error_messages' %>

	<%= f.fields_for :answers do |builder| %>
	 	<%= builder.text_area :answer, { :placeholder => "Antwoord...", :class => "span9", :cols => 30, :rows => 10 } %>
	<% end %>

  <%= f.submit "Antwoord" %>
<% end %>