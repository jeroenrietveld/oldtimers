<h1>New</h1>

<%= form_for(@question) do |f| %>

  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :question %>
  <%= f.text_area :question %>

  <%= f.submit "Post question", class: "btn btn-large btn-primary" %>
<% end %>