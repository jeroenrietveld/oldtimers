<h1>New</h1>

<%= form_for(@question) do |f| %>
	<%= render 'shared/error_messages' %>

  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :question %>
  <%= f.text_area :question %><br />

	<%= collection_select(:question, :categories, Category.all, :id, :name, {}, { :multiple => true , :id => "categories_select"} )%>
	
  <%= f.submit "Post question" %>
<% end %>
